Étape 1 — Récupérer les dernières modifs
git pull origin main
Étape 2 — Installer les dépendances (une seule fois)
Pour le backend :
cd backend
npm install
npx prisma generate


Pour le frontend d'Aziz (eduflow) :
cd frontend/eduflow
npm install --legacy-peer-deps

Pour le lancement du projet  : npm start

Pour le projet de Chrys  :
cd frontend/GESTION_SUIVIE_PROJET
npm install

Pour le lancement du projet  : npm run dev
